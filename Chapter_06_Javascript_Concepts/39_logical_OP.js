// && -> And operator
// || -> Or operator
// ! -> Not operator

let a = true
let b = false

console.log(a && b); // false (both need to be true)
console.log(a || b); // true (at least one is true)
console.log(!a); // false (negation of true is false)
console.log(!b); // true (negation of false is true)